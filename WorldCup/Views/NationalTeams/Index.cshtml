@model IEnumerable<WorldCup.Models.NationalTeam>

@{
    ViewBag.Title = "Index";
}

<h2>National Teams in World Cup 2018</h2>

@if (User.Identity.IsAuthenticated)
{
    <p> @Html.ActionLink("Create New", "Create") </p>
    <p>Welcome @User.Identity.Name [@Html.ActionLink("Log out", "LogOut", "Home")]</p>
}

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Continent)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Information)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Continent)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Information)
        </td>
        @if (User.Identity.IsAuthenticated)
        {
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.NationalTeamId }) |
                @Html.ActionLink("Details", "Details", new { id = item.NationalTeamId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.NationalTeamId })
            </td>
        }
    </tr>
    }

</table>
